# Reusable configurations

Environment settings live in `configs/<environment>/`. Keep credentials and
outputs elsewhere. Physim's evaluation config uses the stock Bash harness and a
trusted local bundle; `release.toml` records the approved license and dataset choices.
